import { DefaultTheme } from "styled-components";

const darkTheme: DefaultTheme = {
    textColor: 'whitesmoke',
    bgColor: 'black',
    accentColor: 'rgb(107, 70, 200)'
};

const lightTheme: DefaultTheme = {
    textColor: 'black',
    bgColor: '#eee',
    accentColor: 'salmon'
};

export { darkTheme, lightTheme };